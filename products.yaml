
openapi: 3.0.0
info:
  title: Product Service API
  version: 1.0.0
  description: Manage and list products and brands.
paths:
  /products:
    get:
      summary: List products
      tags: [Products]
      parameters:
        - in: query
          name: brand_id
          schema:
            type: string
        - in: query
          name: search
          schema:
            type: string
      responses:
        '200':
          description: List of products
